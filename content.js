Array(...document.getElementsByClassName('cost')).map(element => {
    element.style.filter = 'none';
});

Array(...document.getElementsByClassName('blured')).map(element => {
    element.style.filter = 'none';
});

Array(...document.getElementsByClassName('havaaleh-blured-detail')).map(element => {
    element.style.filter = 'none';
});